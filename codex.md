# 可噜记账：产品文档与项目长期约定

> 文档状态：第一版需求已确认  
> 产品名称：可噜记账  
> 目标平台：Windows  
> 文档用途：本文件同时是产品需求文档、技术决策记录和整个项目期间必须遵守的协作约定。

## 1. 产品概述

可噜记账是一款面向个人用户的桌面记账应用，用于快速记录、查找和分析日常人民币收入与支出。第一版完全离线运行，账目只保存在用户自己的电脑中，不要求注册、登录或连接服务器。

### 1.1 第一版目标

- 在 Windows 电脑上安装并运行。
- 快速记录每一笔人民币收入与支出。
- 使用相互独立的两级分类整理收入与支出。
- 查询、筛选、编辑和删除历史流水。
- 查看月度收入、支出、结余、趋势和分类占比。
- 使用 CSV 导入或导出流水。
- 创建完整数据备份，并在需要时恢复。
- 提供简洁温暖的浅色界面，同时支持深色模式。

### 1.2 第一版不包含

- 转账、借贷和多币种记账。
- 多人账本、家庭共享或团队协作。
- 账号系统、云端存储和跨设备自动同步。
- 预算管理和超支提醒。
- 票据图片、拍照识别或 OCR。
- 自动更新功能。

以上能力可作为后续版本候选项，但不得默认加入。需要加入时，必须依照本文件的技术决策流程向用户提供方案并获得选择。

## 2. 整个项目期间必须遵守的协作约定

本节是强制性长期规则，适用于产品设计、开发、测试、打包、发布和后续维护的全部阶段。

### 2.1 用户与开发方的职责

- 用户不懂编程技术，也不需要学习编程后才能参与项目。
- 用户负责选择产品效果、使用体验和可理解的方案取舍。
- 不得要求用户编写代码、选择底层技术参数、阅读技术日志或亲自解决技术故障。
- 开发方负责发现技术问题、准备可行方案、解释影响、实施用户选定的方案并完成验证。

### 2.2 技术决策流程

凡是可能明显影响产品体验、开发成本、运行性能、用户隐私、安全、兼容性、维护难度或后续扩展的技术事项，开发方必须：

1. 先用不依赖编程知识的语言说明需要决定的问题及其对用户的影响。
2. 提供若干真正可行的互斥方案；通常提供 2 至 3 个，不提供无意义的陪衬选项。
3. 对每个方案分别说明：
   - 用户最终会获得什么效果；
   - 优点；
   - 缺点；
   - 大致开发和维护成本；
   - 主要风险或限制；
   - 适用场景。
4. 明确给出推荐方案和推荐理由，但不能替用户作出重要选择。
5. 等用户根据产品效果和取舍作出选择后，再实施该项决定。
6. 将会长期影响项目的决定补充到本文件，保证后续工作保持一致。

未经用户选择，不得擅自采用会显著改变上述因素的方案。对于不会改变产品行为、风险低且容易撤销的内部实现细节，开发方可以选择成熟、稳定、易维护的做法，并在阶段结果中用通俗语言简要说明。

### 2.3 完成与变更规则

- 每完成一个开发阶段，都必须进行与风险相称的实际检查或测试；只有验证通过后才能向用户宣称完成。
- 不能把计划执行、代码编写完成或命令已发出当作验证成功。
- 如果当前环境不能完成某项验证，应明确说明哪些内容已完成、哪些仍未验证以及原因。
- 如果新需求与本文件冲突，应先解释冲突、影响和可选处理方案，获得用户确认后再更新文档和实现。
- 不得在未说明影响的情况下静默扩大第一版范围。

## 3. 技术方案比较与已选方案

### 3.1 候选方案

| 方案 | 用户获得的效果 | 优势 | 劣势 | 开发与维护成本 | 主要风险或限制 |
| --- | --- | --- | --- | --- | --- |
| Electron + React | Windows 和 macOS 上体验一致的现代桌面应用 | 生态成熟、跨平台稳定、第三方组件丰富、开发和维护方便 | 安装包较大，运行时内存占用通常较高 | 中等；资料和成熟方案较多 | 需要控制应用体积和后台资源占用 |
| Tauri + React | 同样的跨平台桌面界面，但安装包和资源占用更小 | 安装包小、运行较轻、安全边界较好 | 开发、原生能力接入和跨平台打包更复杂 | 中等偏高 | 不同系统上的原生依赖和打包问题更多 |
| Flutter Desktop | Windows 和 macOS 高度一致的自绘界面 | 性能良好、界面一致性强、适合定制视觉 | 桌面生态和现成组件相对前两者少 | 中等偏高 | 部分桌面能力可能需要自行适配或寻找插件 |

### 3.2 最终选择

第一版采用：

- Electron：桌面应用外壳和系统能力。
- React：构建应用界面。
- TypeScript：减少开发中的类型错误，提高维护性。
- SQLite：在用户电脑上保存结构化账目数据。
- Electron Builder：生成 Windows 安装包。
- Radix UI + Tailwind CSS：构建可访问、可定制的界面与浅色/深色主题。

选择 Electron + React + TypeScript 的原因是其跨平台方案成熟、资料和组件丰富，适合稳定、快速地构建本项目。其安装包和内存占用较大的缺点已知并接受，开发时应避免引入不必要的依赖并控制后台资源使用。

界面组件采用 Radix UI + Tailwind CSS。选择原因是能够在保留成熟交互与无障碍能力的同时，做出符合“简洁温暖”定位的定制视觉，避免应用呈现通用企业后台或固定的 Material Design 风格。

## 4. 产品信息架构与核心页面

### 4.1 首页

- 展示本月收入、本月支出以及收入减去支出后的结余。
- 展示最近的收入与支出记录，并明确区分类型。
- 提供醒目的“记一笔”入口。
- 无账目时展示友好的空状态和首次记账引导。

### 4.2 新增与编辑收支记录

每笔记录包含：

- 收支类型：收入或支出，必填。
- 金额：必填，人民币，必须大于 0，最多两位小数。
- 一级分类：必填。
- 二级分类：必填，必须属于已选一级分类。
- 日期时间：必填，默认当前时间并允许修改。
- 备注：选填。
- 支付或收款方式：选填，默认使用用户在设置中选定的方式。
- 商家或来源：选填。

保存成功后应给予明确反馈。编辑时显示原有信息；删除属于破坏性操作，必须二次确认，并明确告知删除对象。

### 4.3 流水页

- 默认按日期时间倒序显示，按日期分组。
- 支持按时间范围、一级分类、二级分类和支付方式筛选。
- 支持按收入、支出或全部收支筛选。
- 支持通过商家或备注关键词搜索。
- 支持查看详情、编辑和删除。
- 清楚展示当前筛选条件，并能一键清除筛选。

### 4.4 统计页

- 展示所选月份的总收入、总支出和结余。
- 允许切换查看每日收入或支出趋势。
- 分别展示收入和支出的一级分类占比。
- 统计结果必须与流水筛选和数据库记录保持一致。
- 对零支出、只有一天数据和缺少上月数据等情况提供合理显示，不产生无意义的百分比。

### 4.5 分类管理

- 展示默认分类和用户创建的分类。
- 允许新增一级分类及其二级分类。
- 允许新增二级分类、改名和排序。
- 允许停用不再使用的分类；停用后不能用于新账目，但历史账目继续正常显示。
- 删除分类不得删除历史账目。已被账目使用的分类只能停用，或先将关联账目迁移到其他分类后再删除。

### 4.6 数据管理

- CSV 导出：按固定列名导出 UTF-8 文件，确保中文可正确读取。
- CSV 导入：提供模板，导入前预览有效记录、疑似重复记录和错误记录，不直接覆盖现有数据。
- 完整备份：包含全部账目、分类和设置。
- 恢复备份：执行前说明影响并二次确认；覆盖当前数据前自动创建当前状态的安全备份。
- 恢复失败时不得留下部分恢复或损坏的数据。

### 4.7 设置

- 主题模式：浅色、深色、跟随系统。
- 默认支付方式。
- 货币说明：第一版固定为人民币。
- 数据保存位置和隐私说明。
- CSV 导入导出及完整备份恢复入口。

## 5. 默认分类设计

| 一级大类 | 二级小类 |
| --- | --- |
| 餐饮 | 早餐、午餐、晚餐、零食饮料、外卖、聚餐 |
| 交通 | 公共交通、打车、加油、停车、车辆保养、长途出行 |
| 居住 | 房租、房贷、物业、水费、电费、燃气、维修 |
| 购物 | 日用品、服饰、美妆、数码、家居、其他购物 |
| 娱乐 | 电影演出、游戏、旅游、兴趣爱好、会员订阅 |
| 医疗健康 | 看病、药品、体检、健身、健康用品 |
| 教育 | 书籍、课程、考试、学习用品 |
| 人情往来 | 礼物、红包、请客、捐赠 |
| 家庭 | 育儿、老人、宠物、家庭公共支出 |
| 其他 | 手续费、罚款、临时支出、未分类 |

收入使用独立的默认分类：

| 一级大类 | 二级小类 |
| --- | --- |
| 工资收入 | 基本工资、奖金、津贴补贴、加班收入 |
| 经营收入 | 商品销售、服务收入、副业收入、项目收入 |
| 投资收益 | 利息、分红、基金股票、理财收益 |
| 退款报销 | 购物退款、费用报销、保险理赔 |
| 人情收款 | 红包、礼金、亲友转入 |
| 资产处置 | 二手出售、资产转让 |
| 其他收入 | 奖励、意外所得、未分类 |

## 6. 支付方式

默认支付方式包括：

- 微信支付
- 支付宝
- 银行卡
- 现金
- 其他

用户可以在设置中指定默认支付方式。第一版不统计账户余额，也不把支付方式当作银行账户管理。

## 7. 数据模型与业务规则

### 7.1 收支记录

每笔收支在数据层至少包含：

| 字段 | 含义与规则 |
| --- | --- |
| id | 全局唯一编号 |
| entryType | `expense` 表示支出，`income` 表示收入；旧版数据升级后默认为支出 |
| amountInCents | 金额，以“分”为单位的正整数保存 |
| primaryCategoryId | 一级分类编号，必填 |
| secondaryCategoryId | 二级分类编号，必填且必须属于对应一级分类 |
| occurredAt | 实际花销日期时间 |
| note | 可选备注 |
| paymentMethod | 可选支付方式 |
| merchant | 可选商家名称 |
| createdAt | 记录创建时间 |
| updatedAt | 最近修改时间 |

金额统一以“分”的整数形式存储，界面以“元”显示，避免小数运算造成金额误差。展示和导出时固定使用人民币符号或人民币说明。

### 7.2 分类关联规则

- 一级分类和二级分类均使用稳定的唯一编号关联账目，改名不会破坏历史记录。
- 二级分类必须且只能归属于一个一级分类。
- 停用分类应保留名称和历史关联。
- 历史账目即使关联已停用分类，也必须可以搜索、统计和导出。

### 7.3 本地数据与安全边界

- SQLite 数据库只允许 Electron 主进程访问。
- React 界面通过预加载脚本暴露的受限制接口与主进程通信。
- 禁止界面中的网页代码直接访问数据库或任意本机文件。
- Electron 应启用上下文隔离，并关闭渲染进程中的 Node.js 直接访问能力。
- 第一版依赖操作系统用户权限保护本地数据，不额外进行数据库加密。
- 应在设置页用通俗语言说明数据仅保存在本机，以及用户需要主动备份才能在设备损坏后恢复。

## 8. CSV 与备份规则

### 8.1 CSV

- 编码固定为 UTF-8；为兼容常用表格软件，可在导出时采用 UTF-8 BOM。
- 提供固定列名和示例导入模板。
- 至少包含唯一编号、收支类型、金额、一级分类、二级分类、日期时间、备注、支付或收款方式和商家或来源列。
- 旧版不含收支类型的 CSV 继续兼容，并统一按支出导入。
- 导入时校验金额、日期、必填分类和字段格式。
- 通过唯一编号以及必要的内容比较识别疑似重复记录。
- 导入确认页分别显示可导入、疑似重复和错误记录及原因，由用户确认后才写入数据库。
- 导入过程必须使用事务，发生错误时整体回滚，不留下半批数据。

### 8.2 完整备份与恢复

- 完整备份包含账目、分类、设置和备份格式版本号。
- 恢复前验证文件类型、格式版本和基本完整性。
- 恢复操作必须先自动备份当前数据，再替换现有数据。
- 恢复过程使用安全的临时文件或事务机制；验证成功后才切换为恢复后的数据。
- 不支持的未来版本备份不得强制导入，应提示原因并保持现有数据不变。

## 9. 视觉与体验原则

- 整体风格简洁、温暖、友好，避免传统财务软件过于严肃或信息拥挤的感觉。
- 浅色模式以柔和的中性色为底，使用适量暖色作为重点色。
- 深色模式保持足够对比度，不能只简单反转颜色。
- 金额、日期、分类和主要操作应具有清楚的视觉层级。
- 危险操作使用一致的警示样式；统计图表不能只依赖颜色传递信息。
- 支持常见桌面窗口尺寸和键盘操作，缩小窗口时主要功能仍可使用。
- 界面文案使用普通用户能理解的中文，避免暴露数据库、进程、IPC 等技术术语。

## 10. 验收与测试计划

只有以下相关测试实际通过后，对应功能才能视为完成：

### 10.1 Windows 与安装

- 在受支持的 Windows 版本上验证安装、首次启动和正常退出。
- 验证应用升级或重新安装时的数据保留行为。
- 验证卸载行为与提示，避免用户误以为卸载必然保留或删除账目。
- 验证生成的 Windows 正式安装包，而不只验证开发模式。

### 10.2 记账与流水

- 验证新增、编辑、删除和查看详情。
- 验证金额为零、负数、超过两位小数、超大金额和非数字输入。
- 验证日期、必填分类及二级分类归属关系。
- 验证按日期、分类、支付方式和关键词筛选，以及组合筛选和清除筛选。

### 10.3 分类

- 验证所有默认分类正确初始化。
- 验证自定义一级和二级分类的新增、改名、排序和停用。
- 验证已使用分类不能直接删除。
- 验证分类停用或改名后，历史账目、筛选和统计仍然正确。

### 10.4 统计

- 验证跨月记录、本月总额、每日趋势、上月对比和分类占比。
- 验证无数据、只有一天数据、上月为零和跨年月份。
- 使用已知测试账目人工核对统计结果，确保总额与流水一致。

### 10.5 导入、导出与备份

- 验证包含中文、逗号、引号、换行和特殊字符的 CSV。
- 验证正确记录、重复记录、缺失字段、错误金额和错误日期混合导入。
- 验证导入失败时数据库不产生部分写入。
- 验证完整备份、正常恢复、损坏文件、未知格式版本和恢复失败回滚。
- 恢复后核对账目数量、金额合计、分类和设置的一致性。

### 10.6 界面与离线能力

- 验证浅色、深色和跟随系统三种模式。
- 验证不同窗口尺寸、长文本、空状态和常用键盘操作。
- 在断开网络的环境中验证第一版全部功能均可正常使用。

## 11. 已确认的假设与后续候选方向

### 11.1 已确认假设

- 第一版记录人民币收入和支出，但不处理账户之间的转账。
- 第一版为单人、单账本。
- 第一版不设账号和密码。
- 数据仅存放在当前电脑上，不自动同步到其他设备。
- 本地数据库默认不加密，由操作系统账户权限提供基础保护。

### 11.2 后续候选方向

- 自动更新。
- 云同步与跨设备使用。
- 月度预算和超支提醒。
- 收入、转账和账户余额。
- 票据图片及智能识别。
- 多账本或家庭共享。

这些内容没有进入第一版承诺。评估任何候选方向时，必须再次提供通俗的产品和技术方案、成本、风险及推荐意见，由用户选择后方可进入计划。

## 12. Git 安全存档与质量门禁

- 正常创建 Git 存档点时，优先调用项目专用的 `gitcommit-agent`。
- `gitcommit-agent` 必须并行调用 `kelu_unit_tester` 和 `quality-engineer`；单元测试与质量检查均形成有效的本地凭证后，Git 才允许提交。
- 质量门槛采用增量规则：全项目已确认的严重问题会阻断提交；本次改动新增严重或高风险问题会阻断；与本次改动无关的既有高风险应继续报告，但不阻断合格的无关提交。
- 检查凭证必须绑定当前 HEAD 和已暂存代码指纹，有效期为 60 分钟。检查后代码发生变化时必须重新检查，不能沿用旧凭证。
- 真正执行每一次 `git commit` 前，都必须向用户展示文件范围、测试与质量结论以及提交说明，并再次获得确认。
- 每次提交前都由用户选择“仅本地提交”或“提交并推送 GitHub”，不能根据上一次选择自动推送。
- 不得使用 `--no-verify` 绕过项目 Hook。Hook 拦截提交时，由开发方解释原因、定位问题并准备修复方案，不能要求用户自行处理技术故障。
- 每轮检查结束后都必须清理隔离快照和检查凭证：提交成功由 `post-commit` Hook 自动清理；检查失败、用户取消或提交失败由 `gitcommit-agent` 清理。清理不得取消暂存、删除代码或改写 Git 历史。
